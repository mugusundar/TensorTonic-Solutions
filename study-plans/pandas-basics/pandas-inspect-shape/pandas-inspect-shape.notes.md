The `df.columns` attribute returns an `Index` object containing the column labels. The Index behaves like an immutable array.



df.shape returns a tuple. 




The `df.dtypes` attribute returns a `Series` mapping each column name to its data type